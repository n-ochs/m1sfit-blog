import React from 'react';

const BroadcastTest: React.FC = () => {
	// const { isLoading, isSuccess, data } = useQuery({ queryKey: ['single-broadcast', broadcastId], queryFn: () => getSingleBroadcast(broadcastId), retry: false, refetchOnWindowFocus: false });
	return <div>BroadcastTest</div>;
};

export default BroadcastTest;
